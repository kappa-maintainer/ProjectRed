package {{ package }}

object ProjectRedTags {
  final val MOD_ID = "{{ mod_id }}"
  final val MOD_NAME = "{{ mod_name }}"
  final val VERSION = "{{ mod_version }}"
  final val SCALA_VERSION = "{{ scala_version }}"
  final val MINECRAFT_VERSION = "{{ minecraft_version }}"
  final val TJPCORE_VERSION = "{{ tjpcore_version }}"
  final val CCL_VERSION = "{{ ccl_version }}"
  final val FMP_VERSION = "{{ fmp_version }}"
  final val JEI_VERSION = "{{ jei_version }}"
}
